 
 CREATE TABLE student(
  Firstname       VARCHAR(20)     NOT NULL,
  Surname         VARCHAR(20)     NOT NULL,
  Algebra         DECIMAL(10,2)   NOT NULL,
  Calculus        DECIMAL(10,2)   NOT NULL,
  Programming     DECIMAL(10,2)   NOT NULL,
  Databasess      DECIMAL(10,2)   NOT NULL,
  PRIMARY KEY (Firstname)
);